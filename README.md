**Step 0**: intialization of python virtual env
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```
> create `.venv` folder to store env details and pip download by `requirements.txt`